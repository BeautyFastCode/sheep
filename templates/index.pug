//-
  The index page.

doctype html
html(lang="en")
    head
        // Required meta tags
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")

        // Custom google font
        link(href="fonts/woff.css", rel="stylesheet")

        // All styles, including Bootstrap CSS
        link(href="css/styles.css", rel="stylesheet")

        // Project title
        title Project Sheep!

    body
        include _navbar

        include _cover
        include _info
        include _counters
        include _lessons
        include _get-start
        include _subscribe
        include _friends

        include _footer

    // Optional JavaScript
    // jQuery first, then Popper.js, then Bootstrap JS
    script(src="js/jquery.min.js")
    script(src="js/popper.min.js")
    script(src="js/bootstrap.min.js")

    // Your custom JavaScript start here
    script
        include ../js/navbar.js
